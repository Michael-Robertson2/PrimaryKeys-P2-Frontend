name: NPM CI

on:
  push:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Source Code
      - uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      - name: Build Artifact
        run: npm run build
